{
  "name": "markdown-parser",
  "version": "0.0.1",
  "description": "Lightweight markdown parser",
  "main": "index.js",
  "scripts": {
    "convert": "node index.js",
    "jest": "jest --silent ",
    "jest:watch": "jest --watch --no-coverage",
    "lint": "eslint lib/**/*.js tests/**/*.js **/*.js",
    "lint:fix": "pnpm lint --fix"
  },
  "keywords": [
    "nodejs",
    "markdown",
    "parser"
  ],
  "author": "Kiran Parajuli",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=14.0.0",
    "pnpm": ">=3.0.0"
  },
  "devDependencies": {
    "eslint": "^8.19.0",
    "eslint-plugin-import": "^2.26.0",
    "jest": "^28.1.2"
  }
}
